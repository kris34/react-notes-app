import React from 'react';
import {Text, View} from 'react-native';

const InsideFolder = () => {
  return (
    <View>
      <Text>This is inside the folder</Text>
    </View>
  );
};

export default InsideFolder;
